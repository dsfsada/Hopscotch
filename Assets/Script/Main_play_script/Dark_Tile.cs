using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Dark_Tile : MonoBehaviour
{
    // Start is called before the first frame update
    public void DestroyThis()
    {
        Destroy(gameObject);
    }
}
